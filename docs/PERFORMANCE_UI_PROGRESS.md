# ICSTeX 性能与 UI：当前交接

最新任务是用户确认六项痛点后的普通写作UI局部优化。修改内容与前后截图见
`writing-ui-refinement-2026-09-14.md`；当前源码、最终回归与安装状态以PROJECT_STATE为准。
以下记录为之前的性能/PDF任务，不自动重开已经延期的验收。

2026-09-14 · 用户将剩余范围明确缩为1输入卡顿、2 PDF/Qt排查、5本机更新。
拼音候选、纯键盘菜单和VoiceOver延期，不再作为本次交付门槛；不把它们标为通过。

## 最新：第2项根因修复

用户追加要求彻底处理第2项后，已建立白页的确定失败时序，并隔离不同PDF的渲染队列、
迟到结果和销毁顺序。详细红绿、失败候选及Qt源码依据见
`pdf-render-lifecycle-verification-2026-09-14.md`；最终回归和本机更新已完成，身份以PROJECT_STATE为准。
以下1/2/5记录描述上一次交付，不替代这次新修复的证据。

## 上一批：1、2、5项收尾

证据目录：任务根下`closeout-125-20260914/`。本次采用最小改动，无新依赖、
渲染器替换、全局GC/线程参数调整，也不改变FINAL质量或论文原稿。

| 同一200子文件、30次原生输入、250ms间隔 | 修改前 | 输入修复后 |
| --- | ---: | ---: |
| 输入至文本像素p50 | 7.008ms | 12.207ms |
| p95 | 50.757ms | 13.483ms |
| 最大值 | 52.936ms | 14.366ms |
| 超过50ms | 3/30 | 0/30 |
| 每次输入的两状态库路径解析 | 5次 | 0次 |
| 整段输入及收尾的成员计算完成次数 | 28次 | 1次 |
| 末键至成员状态就绪 | 374.184ms | 729.679ms |

`input-before`显示慢样本中两状态库解析耗时约44–46ms，GUI CPU不足1ms。
已登记的规范root现在直接查内存记录，未知路径/别名仍解析；首次登记、不同root、
别名改指向和revision/build保护保留。仅做这一改动的`input-after`仍有58.449ms p95，
不能算整体完成。继续将项目成员扫描和后台统计改为停笔500ms后合并执行，显式保存/
编译仍可立即校验依赖，实际最终输入证据为`input-final`。中位数没有改善，收益是
减少尾部卡顿；这些是Qt事件到控件像素上界，不是物理键盘/compositor测量。
输入被测app为`61b2b05ec31d4deeae4afe10ed28b16f47bb0b0ea4086c1fcb648d4d58c980ba`。
后续唯一app变动是下述PDF计数回调，不触及这条无PDF输入路径，未重做输入矩阵。
`input-events`、`input-idle-after`与`input-idle-confirmed`的前台失活/未曝光失败保留，
未将残缺样本纳入通过统计。

PDF原生`pdf-lifecycle-final`覆盖首次显示、无变化复用、同路径替换新字节、切root、
6次搜索中清空/重开以及搜索未完成时关窗。新颜色像素、3页/3匹配、阅读位置/缩放、
清空及销毁都有断言；捕获先要求自然Paint，不用强制repaint冒充显示。
发现并修复真实计数显示缺陷：保留阅读位置的reload不自动选中匹配时，异步count变化
未刷新工具栏，可能停在0/2而模型已为3；现在正常显示0/3且不跳页。
早期探针更换查询导致跳到首个匹配，是探针前提错误，`pdf-lifecycle`原始失败保留。
小测试原固定250ms不足以完成异步搜索，改为有界等待最终3匹配，不修改产品等待时间。

旧`Python-2026-09-12-224226/224422.ips`搜索SIGSEGV已与历史日志核对：原测试故意
单独释放QPdfDocument却保留Qt消费者，夹具修正已有红绿记录，不是新的产品崩溃。
`Python-2026-09-13-010730.ips`启动SIGABRT与独立测试缺少QApplication的历史记录分列。
原A首次空白没有保留失败PDF或窗口曝光状态；不能还原其因果，也不宣称彻底根除。
当前原生刷新/搜索/销毁通过，保留该不可复现的历史风险，不用无限复跑替代结论。

当前最终app：`d8292f5bdd68737e4f34b11e0c244d2a0412957cc43b8632d84c5eca0e04be38`。
Physics EE冻结副本的自动保存→PREVIEW→新颜色像素回归通过：首轮5.476s、暖轮3.015s，
26页、build/revision、保存字节、统计最终完成及销毁均有断言。该离屏单轮与最终回归
并行，仅用于确认路径未破坏，不据此宣称比上批7.03s又快一倍。原生尝试在注入前失去
前台，`real-auto`失败保留，改用`real-auto-offscreen`；没有改动EE原件。
最终preflight通过1722项/278.153s，命令279.726s、退出0，compileall通过；app+tests
`c52f6ca09460a1dd101a3f163e29cbb8cae3c928f94719ef385c9007ce9e2352`前后相同。
日志保留Qt平台提示及临时目录关闭后的异步诊断，不声称无警告。
本地新包已完成备份/安装/启动，实际201个app模块+入口及36项资源符合源码，
arm64/ad-hoc签名通过，与旧包模块集相同；既有13个未打包模块明确列于收据。
程序摘要`6a8843ffae83540119ebdcf415f8cab97766c357e518589159867e7b3ddcc267`。
旧版完整保留，先确认EA已保存/编译空闲再正常退出；新版已重新打开原EA文件，已保存/
空闲，没有写入或重新编译该文稿。详情为`installed-receipt.json`与`installation.md`。
1及5完成，2当前流程验证通过但旧空白因果未证实；不自动将Goal标为彻底完成。
3/4明确延期。停止重复基准与验收矩阵；不提交、推送或公开发布。

## 上一批：任务主按钮状态

从已有primaryButton入口核查了14类实际控件。首批7类均能获得离屏焦点但焦点外观
不变，检查点/恢复/迁移/中断写入恢复的禁用状态也与可点状态相同；其余引用、历史、
图片、模板、表格、普通诊断和环境报告同样存在状态覆盖。已给现有实例绑定同一局部
状态样式，不改启用条件、信号连接或事务；未引入全局QSS扫描/新状态系统。

随后复合状态断言证明，鼠标已悬停时按下没有进一步反馈。普通主按钮与工具栏主动作
现在使用更深的同色系按下色，普通/悬停和字体/尺寸不变；旧公式样式名仍兼容。
14类×五档共70组状态检查通过，普通像素与改前一致、几何稳定、clicked计数为0。
控件外观状态由测试显式设置，不代表业务准备条件或发布/恢复流程已经允许执行。

证据根下`task-button-states-20260914/`：`before/`及`remaining-before/`保留失败，
`all-after/`和`final/`记录五档状态与截图，`native/`保留首轮未获焦点结果，`full/`
保存完整回归。原生失败后前台工具确认Mac锁屏；未自动解锁或强制接管，已询问用户。
新增active/exposed前置条件只使后续检查有效，不改写原失败。待解锁后补原生焦点；
其余真人IME/菜单/VoiceOver和历史风险仍开放，不以按钮外观通过关闭。
本批首次原生等待后两次复核仍锁屏，曾按三轮规则标blocked。用户再次要求继续后，
前台工具已确认可用；`native-unlocked/`补原生状态确认通过，70组窗口均active，
焦点/按下/禁用与几何、零业务点击通过。原失败未抹去，也未重跑完整回归或性能矩阵。
随后准备过独立人工测试窗口，原进程现已退出，文件保留。用户明确要求由agent自己
执行测试，故不再把输入操作交给用户：同入口新增`--output-name agent-ime --observe`，
使用新副本及只读候选/提交/保存观察。用户解锁后，agent已通过前台工具自行完成中文/
emoji粘贴保存、Undo/Redo、公式源码草稿隔离及Esc取消，01–06快照验证源文本与磁盘；
测试副本已恢复、进程正常退出。详情为`agent-ime/result.json`。
按键通道仍只提交Latin文本，系统TextEdit对照也相同；未捕获QInputMethodEvent，不能
将粘贴当作拼音候选/取消通过，也不据此修改产品代码。Ctrl+F2菜单入口未观察到成功。
系统设置仅查看、当前输入法与开始时相同。TextEdit对照自动存入iCloud的8个测试字母
已通过UI移回本地，旧路径不存在；不涉及论文，也不保证云端历史从未存在。

## 上一批：边际收益评估与欢迎页补缺

本轮参考门槛是端到端约10%的额外下降，不通过降低画质或放松FINAL校验取得数字。
阶段诊断在8dd861b0源码上运行，暖样本3.043s：保存前等待约0.869s，编译器报告1.863s
（含32.6ms代理准备），静态检查约109ms，PDF加载约7.8ms。该机器时段比上一批更快，
但源码未改，不能把7.03→3.04s包装为新优化；单样本和诊断开销也不作性能验收。

唯一追加参数实验为固定XDV的压缩1→0，按1/0、0/1、1/0交替运行。中位456.775→360.967ms，
只省95.808ms；临时PDF7.35→12.53MB（约71%），26页72dpi渲染相同。即使将该段节省
全部计入，也只约占上述暖诊断样本的3%；不采用，生产编译设置仍为上一批的快速无损压缩。
这不是声称引擎已经达到性能上限；只是在当前低风险路径停止投入，按用户要求转UI。

| UI补缺 | 之前 | 当前 |
| --- | --- | --- |
| 重启后的欢迎页 | 已保存的展开工具箱显示整个用户目录，挤占欢迎内容 | 无项目时临时收起；进入写作恢复偏好，关闭欢迎页不覆盖偏好 |
| 宽窗变窄 | 四列布局的空列仍保留伸展，约半屏空白 | 清除无用列伸展；复用原控件，重排不丢按钮焦点 |
| 欢迎主动作状态 | 键盘焦点和普通状态像素相同 | 复用已有局部主按钮样式，focus/pressed/disabled可辨且几何不变 |

证据根下`preview-next-20260914/`：`phase-profile/`为诊断，`compression-zero/`为未采用
实验，`ui-before/`与`ui-final/`为同尺寸离屏对照，`ui-native/`为原生确认，`full/`为
当前完整回归收据。测试覆盖偏好跨重启、显式打开工具箱/文件夹、五档两尺寸按钮布局、
焦点/按下/禁用像素及原源码/Block面板行为。原生为Qt程序化焦点/状态检查，不代替
真人IME、系统菜单键盘或VoiceOver。安装版仍是8dd861b0，本轮没有替换正在使用的应用。
本批实际源码修改仅为六个GUI模块：`main_window.py`、`project_file_controller.py`、
`responsive/source_panels.py`、`responsive/helpers.py`、`theme/__init__.py`和`welcome_page.py`；
旧公式主按钮样式名保留兼容别名。原稿、编译核心和共享工作区其他增量未更改。
下一步是原Goal剩余F人工验收及新的具体UI反馈，不将本批欢迎页检查称为全部C–F完成。

## 上一批自动编译修复

用户指定的Physics EE仅在隔离副本中测量；原始主文件SHA-256为
`ab4179b6569ca117f9197d0e31d451c7c5f4ac48cb9f4dd0c29084432858b750`。
保留用户原有XeLaTeX自动选择、800ms保存防抖、1200ms外部编译防抖和150%缩放。

| 同项目、同口径结果 | 修改前 | 当前修复 |
| --- | --- | --- |
| 编辑事件→新PDF像素，离屏5次中位 | 9.962s | 7.035s（减少29.385%） |
| 同组最大值 | 10.387s | 7.996s |
| 原生确认，3次暖预览 | 未重做旧版原生对照 | 6.744 / 7.283 / 7.672s |
| 首次预览，单次，不计入暖样本 | 13.843s | 离屏12.025s、原生12.469s |

结束点要求新颜色像素、当前build/revision及输出路径一致，非状态标签或进程退出。
这是源码GUI副本测试和截图观察上界，不是所有项目、物理键盘或compositor精确时间。
起点在编辑器setPlainText之前；各次只变更副本注入的页边颜色标记，自动保存与编译
由产品原有定时器触发。没有在计时中手动调用保存/编译，也没有修改正文来制造收益。
首次预览仍需建立图片代理等缓存；不承诺首次打开也只等7秒。

实际保留的最小改动：已有依赖快照不再为自动PREVIEW重复强制检查；成功原子保存的
字节立即登记，防止本机保存重新走外部修改防抖；后台字数统计让位于保存/排队/编译
及PDF首轮显示，手动统计仍可发起；XeLaTeX预览采用快速无损压缩，FINAL与受限路径不改。
固定XDV交替压缩对照中位3.227→0.946s，26页72dpi渲染逐页像素一致，临时PDF约大6.36%。
第一个候选完整中位12.001s、只补保存回声后9.406s，均保留原始收据，不作为成功交付。

证据位于下述证据根的`auto-compile-20260914/`：`before/`、`after/`、
`after-save-echo-fix/`、`compression/`、`after-fast-compression/`、`native-confirmation/`。
复现入口为`tools/bench_auto_compile_latency.py`及`tools/probe_preview_compression.py`。
新原生确认已结束并销毁独立测试窗口，未操作原稿。完整preflight与修复包源码/
签名校验通过。首次安装遇到锁屏及旧进程仍运行后暂停；用户继续后已确认旧进程退出，
保留备份、替换本机应用并观察新版本欢迎页/latexmk就绪。没有重复性能或完整回归，
收据为`local-install-2.1.0-beta.2-8dd861b0/source-receipt.json`。现在交回用户验收。

## 前一轮范围与交接背景

范围仍为已批准的[执行Prompt](/Users/leo.xu/.codex/visualizations/2026/07/13/019f5c12-2ce0-7713-bae4-e9f2277ae371/icstex-performance-ui-2026-09-12/ICSTeX-性能与UI优化-Codex执行Prompt.md)：
Mac本地Python/PySide6源码、验证证据与人工清单；不是V1发布。
用户已另行批准最小路径的结构性样式调整。旧FORCODEX发布brief原样保留。
之后用户单独授权本机更新：新增范围仅为本地构建、备份、安装和启动，不含远端发布。

仓库：`/Users/leo.xu/Desktop/Codex/ICS-Project-/ICSTeX`。
分支`codex/v1-development`，HEAD `3bde2b5d25803262dedef89a081ee6ffbe2b6967`。
工作区包含既有未提交增量；未清理、提交、推送或升级依赖。仅本机应用已按新增授权更新。
最后增量为`main_window.py`、`document_lifecycle.py`及相关回归：注册/保存时确定
路径身份，输入复用它；不改原子写入目标或正式编译校验。完整源码摘要与收据见
[PROJECT_STATE](PROJECT_STATE.md)。当前app前缀`3897ec74`，app+tests前缀`32fcb913`；
HEAD不能独自标识工作区源码。下方cddab32e及更早性能/原生收据保留原身份。

## A–F 状态

| 批次 | 当前交付 | 未完成或不实施的边界 |
| --- | --- | --- |
| A 基线/异常 | 分层基准、独立夹具和失败记录齐备；交付探针重入已修 | 历史原生PDF空白/Qt崩溃未因后续通过而关闭 |
| B 响应 | 依赖后台计算/有界合并、PDF字节身份复用、迟到回调保护、紧凑搜索 | 输入尾部及跨轮波动仍需定位；大图FINAL约30%实验目标未达到 |
| C 工作台 | 紧凑源码/Block界面、默认折叠控制台、窄窗切换、分栏偏好保留 | 普通尺寸矩阵已覆盖；真人与完整视觉交互不由截图代验 |
| D 任务面板 | 检查摘要/原因/详情、固定交付阶段与底栏、公式/引用/Block表达、共用审阅布局 | 原事务/固定字节/UNKNOWN/草稿/一次Undo保持；真实人机验收保留清单 |
| E 次级性能 | 字数片段拼接优化、固定QSS五档差异与父到子刷新；默认档不接收无用全局事件 | 启动懒加载、绘制暂停等无明确收益，不采用；非默认新控件和非菜单缩放保护不变 |
| F 验收/交接 | 当前preflight与本机安装通过；历史83状态矩阵/10状态搜索保留身份 | 最新安装版仅确认启动，完整交互、尾部/人工/历史风险仍未关闭，不宣告全Goal完成 |

主要变更入口：`app/gui/dependency_controller.py`、`app/core/dependency_membership.py`、
`app/core/project_dependencies.py`、`app/gui/pdf_panel.py`、`app/core/pdf_identity.py`、
`app/gui/responsive/`、主窗口layout/actions、Block面板、公式/提交检查/固定交付及
恢复审阅对话框、`app/core/word_count.py`和三个既有theme文件。这里不是把全部
Git diff归为本任务：共享工作区的原有增量继续保留。架构依据见D022及原已接受边界。

## 已验证性能与证据边界

| 路径 | 前后或当前结果 | 解释 |
| --- | --- | --- |
| 200-child依赖GUI刷新 | 历史配对timer lateness p95约94.825→1.453ms | 后台化的分层收益，不是TeX引擎提速 |
| PDF无变化PREVIEW | 历史5样本退出→可见中位43.929→16.815ms | 已验证同字节显示复用；5样本不作p95 |
| Word Count英文40k | 历史交替对照1167.283→957.389ms，约18% | 17种完整结果一致；额外峰值分配约2.48MB，不承诺所有文本同幅收益 |
| 单窗口缩放 | 同基准p95约210.34→43.00ms | 每场景30次，离屏同步调用 |
| 源码+Block+公式缩放 | 同基准p95约444.74→101.36ms | 约77%减少；不是原生compositor或编译延迟 |
| 前版原生输入 | 30次p50/p95/max为8.728/43.290/91.936ms，1次>50ms | a66b445d用户确认时段；同原探针口径，live200-child、250ms间隔 |
| 前版输入handler/成员 | handler最大5.135ms；末键→最新成员396.988ms；一次接受提交4.594ms | 不将单次成员提交称p95；旧请求拒绝、队列有界 |
| cddab32e过滤器原生AB/BA | 开启/关闭各30次：p95为16.727/11.935ms，max72.925/12.706ms | 同窗配对差中位-0.768ms，22/30对关闭较快；不是最新路径增量的测量 |
| cddab32e独立原生输入 | p95/max104.527/116.001ms，3/30次>50ms | 在配对前完成；这个未达标结果不被随后配对或旧43ms覆盖 |

输入门槛仍按原Prompt：键入到可见变化p95<50ms；依赖GUI提交p95≤16ms、常规
交互无>50ms阻塞。不能把两者混写成“任何一次可见变化都必须<50ms”，也不能由
handler很快就证明全部GUI事件没有阻塞。前版确认时段最慢样本handler1.423ms、Paint事件
86.797ms、观察完成91.936ms；等待区间尚未得到具体因果结论。

当前与历史样本分开保留：首轮结构版输入p95/max56.002/93.281ms；正常Qt循环
单轮曾12.975/17.938ms，但同窗AB/BA对照仍有74–76ms尖峰，已排除“仅人工sleep”
的解释。因此前版43ms不是生产代码修复的前后对照，也不覆盖任何旧失败。

新增事件诊断定位到MainWindow的UpdateRequest：两个慢样本约71ms实际时间、6–7ms
GUI CPU；多个普通Paint各等待约5–6ms。诊断覆盖notify会增加开销，不作验收数字。
100%档的原全局过滤器始终返回False，后台受控CPU对照确认存在额外事件成本。
生产改动仅在非默认菜单档安装过滤器，回到100%/非菜单档移除；新控件Polish、字体、
样式、GC和草稿保护不变。新回归先失败后通过。保留这处小幅降耗，不宣称独立104ms
长尾已经解决。随后GUI线程限定计时将一次35.714ms等待定位到标签页重复路径解析。
当前已将解析移到注册/保存边界，共享依赖、深层子文件及替换符号链接的归属回归保留。
后续离屏观察中该处调用为0，但预览状态库解析仍有56.035ms等待，未改状态库。
首份cProfile混入后台调用且时间不一致，28条后断言失败，不用于GUI归因，原记录保留。

## 当前收据索引

证据根目录：
`/Users/leo.xu/.codex/visualizations/2026/09/12/01a09525-8ead-7b81-a47b-62c6a9e485ce/icstex-performance-ui/`。
以下为相对此目录的路径。

| 收据 | 覆盖与边界 |
| --- | --- |
| `local-install-2.1.0-beta.2-3897ec74/source-receipt.json` | 当前源码preflight通过，PyInstaller独立目录构建；包内201个模块与源码一致、签名通过，已安装并观察欢迎页启动。旧版备份保留，不等于完整用户验收 |
| `structural/path-identity-fix/full-r2/`、`feedback-expectation.log` | app3897ec74全套仅旧/var路径预期失败；改为chapter.resolve()，其余metadata断言保留，专项通过。新测试摘要不同，未重标为全套绿 |
| `structural/path-identity-fix/` | 重复解析、两个不同root/符号链接、保存路径/本地记录、深层子文件回归；首次兼容性失败与修复均保留 |
| `structural/source-edit-thread-profile/`、`path-identity-fix/profile-after/` | GUI线程限定调用区间；原件/窗口销毁通过，是离屏诊断而非原生输入延迟 |
| `structural/default-filter-fix/full/` | 前版强制unittest命令退出0，前后cddab32e/05a1e070一致；既有警告保留 |
| `structural/default-filter-fix/` | red/green回归、独立native-input-after及同窗native-paired；原件/队列/窗口销毁通过，原始失败保留 |
| `structural/native-input-dispatch-profile/`、`default-filter-isolation/` | 事件位置诊断和受控后台重绘对照；不是原生输入验收或GIL根因证明 |
| `structural/runtime-after/`、`dimensions-after/` | 五档多窗口/草稿/生命周期与83状态离屏；原c3f06d3b收据后仅改模块说明和测试观察点，未改运行逻辑 |
| `structural/native-layout-diagnostic/` | a66b445d的83状态：四尺寸五档源码/固定审阅/公式/Block；实际两类FINAL、固定PDF、原件/草稿/Undo；100%/1440源码viewport661px |
| `structural/native-final-r1/` | 首轮成功编译后ApplicationInactive、窗口仍exposed，布局前置超时；未再注入键盘，原失败保留 |
| `structural/native-search/` | a66b445d五档×两尺寸，三页中文匹配、预编辑保护、Tab/Esc和焦点返回，原件/PDF不变；不是真人IME |
| `structural/native-input-confirmed-window/` | a66b445d的30次输入，原件不变、无编译、窗口销毁、源码摘要一致；不是compositor精确时间 |
| `structural/native-input-wait-paired/` | 每方法30次同窗AB/BA等待方式对照；两方法均有尖峰，不采用该测量试验作为生产优化 |
| `f/native-flow-r2/`、`f/native-build-controls/` | 旧源码30次root归属、像素抽查、启用后Stop/失败/恢复；保留原身份，不改标当前版重测 |
| `b2/pdf-after.json`、`f/final-dependencies.json`、`e2/` | 历史性能分段/冷暖定义、对象/RSS与负面实验；完整解释在归档记录 |

前版确认时段的运行前top第二次1秒采样为66.10%整机idle，WindowServer45.1%单核CPU，
仍有后台活动及148次swap-in；不能称系统完全空闲，更不能直接归因91.936ms尖峰。
用户确认是验收时段的授权，不是机器资源测量。没有关闭其他应用或操作虚拟机。

## 下一步与最短人工清单

用户已反馈性能恢复，本轮按追加指令转入UI补缺；当前结果见上方，不重做下面的
历史矩阵。历史PDF空白/Qt崩溃、输入尾部及完整交互仍未关闭，运行时和FINAL核验不改。
下面清单为延期事项，不是当前自动执行计划。

从仓库运行`python3 -m app`，新建合成文档，不测试论文原件。以下尚未执行，
不计作通过；不用再复跑已覆盖的正常尺寸矩阵：

1. 中文输入法：源码与公式中组合、取消、确认中文/emoji并Undo，检查候选不提前写入、
   不重复、不丢选区或草稿。
2. 键盘/可达性：真实键盘菜单、Tab/反向Tab、Enter/Esc、Control+Tab作用域、PDF搜索
   焦点返回；VoiceOver单独确认，QTest/AX不替代。
3. 视觉/使用：只补悬停、选中、禁用、焦点和长文本/空态/错误阅读，记录实际卡顿或
   视口跳动；固定交付用合成项目审阅UNKNOWN、取消、生成新目录，保留原目录。

## 历史记录

此前947行任务记录已逐字节归档，SHA-256：
`d58a5bc95020b6773b7d4a2dfb0df02e308aa3603f03979cdd389a279271c475`。
[完整旧记录与原失败上下文](/Users/leo.xu/.codex/visualizations/2026/09/12/01a09525-8ead-7b81-a47b-62c6a9e485ce/icstex-performance-ui/progress-history-before-confirmed-window.md)；
工程历史仍在`PROJECT_LOG.md`，旧条目未修改。当前入口不再承载旧“下一步”或旧
源码的“当前”结论；归档不是删除失败，也不改变目标与完成条件。
