# ICSTeX Roadmap

更新时间：2026-09-20（Asia/Taipei）

本文件描述产品与技术路线，不是功能愿望清单。每个进入实施的项目都必须
同时满足用户价值、稳定性、可维护性和可验证性要求。

## North Star

ICSTeX 要成为 ICC 学生从“建立写作项目”到“提交可靠成果”的本地科研写作
基础设施。核心成功标准不是功能数量，而是学生能否更少出错、更快定位问题，
并确信最终 PDF 与当前源码一致。

## Priority Rules

新工作按以下顺序判断：

1. 保护学生论文和交付文件不丢失、不被意外覆盖。
2. 保证编译、PDF、字数和引用状态真实可信。
3. 降低 LaTeX 环境、项目结构和错误诊断的学习成本。
4. 支持 IA、EE、实验报告等完整工作流，而不是增加孤立按钮。
5. 在没有明确学生价值时，不引入新的状态、依赖、网络或维护负担。

## Current Source Task - Formula Navigation; Handwriting Withdrawn

在既有结构编辑器内修正上下标方向、嵌套结构退出和可见光标反馈，保留草稿、Undo、
未知宏源码回退及显式确认。手写识别未达质量要求，按用户“不交付半成品、不为次要
功能膨胀软件”的要求撤下画板与入口；只保留键盘改进和原有图片 OCR 的就绪/启动修复。
用户已确认导航功能可用；追加显示间距优化已通过专项、完整回归和原生可见检查，
并按明确授权备份/替换本机安装版。当前交付见 PROJECT_STATE；提交与公开发布另行确认。

用户随后授权的本地资源/功能覆盖评测已完成：Tiny FP32、Tiny MPS FP16和Small FP32
均能运行，但固定手写子集上未达到可靠整式匹配门槛。未改默认后端，不以关闭编辑器
功能“节省对应权重”。评测记录保留，独立 UniMERNet 试验环境清理；不继续下载更大
模型、训练或接云服务。只有另行确认范围与可用性门槛后才重新考虑手写功能。

## Completed Source Task - Preview Forward SyncTeX

已复用反向定位身份校验，让当前 PREVIEW/FINAL 都可从原始源码正向定位；不要求
新打开的子文件先建立编译管理器，不额外保存或编译。查询前后和延后跳转时仍绑定
同一 root/revision/build/viewer；多文件图片代理、窄写作区域可见定位及完整回归通过。
本轮同时把旧 V1 发布清单标为历史快照；不重做教程、不启动签名/Windows 分发、
学生试用或整套性能实验，不引入 ProjectSession 重构。最终事实与验证见 PROJECT_STATE。
用户已验收正向定位；追加的双击源码目标高亮已通过专项/完整回归并安装本机。
已授权完整有效源码本地提交。远端同步、公开发布与更广的原生/学生验收不在本轮范围。

## Previous Source Task - Guided Writing Exercise

本轮已完成应用内图文教程与独立写作练习：复用已有中文模板和编译/PDF流程，带用户
改标题、主动编译、查看当前 PDF；可收起和继续。重开后保留文件，重新编译确认结果。
使用本地真实界面配图，不自动播放、不引入网络或通用导览框架。普通窗口不启用导引。
源码与最终验证见 PROJECT_STATE。学生试用和公开发布单独安排；本轮结束，不自动重开其他任务。

## Previous Source Task - Local Simplification

本轮已完成检查控制器收尾与共同身份、批量设置的重复信号/写入、PDF 基础文件
可用性规则，以及最近项目与模板预览的无关重复工作。保留现有 UI、编译/文件安全、
PREVIEW/FINAL 和两类检查的业务差异。专项、离屏显示检查及最终完整回归已完成；
结果与边界见 PROJECT_STATE。本轮结束，不自动继续旧功能、全仓库重构或发布。

## Previous Source Task - Student Writing, Batch 1

当前已按最小实现完成创建流程、保存/PDF 状态、错误入口和欢迎页的源码改动及必需回归；
范围与证据见 `student-writing-first-batch-2026-09-16.md`，最终验证状态见
`PROJECT_STATE.md`。复用既有模板、诊断、状态库和 Qt 控件，不改编译调度。
公开 Beta 3、官网和本机安装版不因本批源码变化自动更新。首次全套原生退出的
根因仍未定，保留风险，不将后续未复现升级为“已修复”；本批不是发布推荐。

下一批按用户反馈再接可选的短任务导引：独立副本、按真实编辑/编译/找到 PDF
状态推进，可退出和重新进入。随后安排学生试用，分别记录已配置环境和全新电脑
的首次有效 PDF 耗时、求助与错误恢复；不以合成或离屏测试代替学生验收。
不自动重开历史性能、升级、IME 或其他已延期任务。

## Previous Source Task - Performance and UI Completion

最新验收反馈的两项入口调整已交付：顶部控制台开关覆盖整个下方面板，保留页签；
移除旧“分段函数”插入按钮，保留公式编辑器结构键。当前已验证源码和本机状态见
PROJECT_STATE，不自主追加界面改版或重测性能。

最新用户请求：按已审查的六项视觉痛点优化普通写作界面，完成后由用户验收。范围限于
正文空间、工具栏层级/标签/字级、PDF空状态及控制台收起；沿用现有界面风格与控件，
保持编译性能和文件安全。完成当前冻结源码验证、本地备份/安装后交接，不扩大为
新风格探索、Web原型、动画或延期的IME/VoiceOver项目。源码验证、安装和EA最终显示
已完成；用户桌面授权后同一进程正常打开文稿，没有重复测试。下一步由用户验收，
只根据具体新反馈继续。证据见writing-ui-refinement文档。

最新追加：用户要求彻底处理PDF第2项，已按可复现的队列/迟到图像与销毁缺陷修复。
该冻结源码的最终检查与本机更新已完成，已交付；不再重开其余已延期项目。
本次证据入口为`pdf-render-lifecycle-verification-2026-09-14.md`。

2026-09-14最新范围覆盖以下旧续作说明：只交付1输入尾部优化、2 PDF/Qt有界排查、
5本机最新版同步。停止拼音候选、纯键盘菜单和VoiceOver验收，不由agent自主重开。
输入尾部已通过同口径原生测量；PDF当前刷新/搜索/销毁路径通过，旧缺失夹具的空白
保留为无法归因的历史风险，不能宣称根除。最终preflight和本地备份/安装已完成并交接，
不因旧宽泛清单再追加测试。当前证据与收据以PROJECT_STATE和PERFORMANCE_UI_PROGRESS为准。

用户已确认上一批性能恢复，并追加“继续尝试快速预览，边际收益不明显则转UI”。
本轮已完成一次阶段诊断与压缩1/0交替对照：额外收益小且临时文件显著变大，不采用。
已转入原C/F计划的欢迎页空间、宽窄重排和按钮状态补缺；当前验收/源码身份见
PROJECT_STATE，实验及UI对照见PERFORMANCE_UI_PROGRESS。不得把未修改源码时的
时段变快称为新收益，也不把本轮小范围原生检查冒充完整人工验收。
当前继续C/D统一主按钮状态：已有14类入口已补局部焦点/按下/禁用及复合悬停按下反馈，
普通外观和业务门槛不变。后台验证已通过；用户恢复前台后原生状态确认已补过，旧锁屏
失败保留，不重复性能实验。按用户要求由agent自行操作独立副本，Unicode保存/撤销/
重做及公式Esc取消已有精确断言；拼音候选与纯键盘OS菜单尚未取得证据，不让用户代跑。
完整F人工和历史风险边界保持开放。

2026-09-14用户验收反馈后恢复修改。当前唯一优化指标为源码改动事件到最新PDF内容
可见的完整等待；用用户指定EE的隔离副本对照，原稿不改。先交付消除重复调度、
后台统计让行和预览快速无损压缩；验证结果与未完成项以PROJECT_STATE为准。
已有本机更新授权，不含Git提交/推送/公开发布。取得实际端到端收益和必需回归后交接，
不重做既有UI矩阵或新增界面功能。后续更换引擎、预编译格式等另行按实测瓶颈评估。

## Previous Source Task - Performance and UI

以下为前一轮范围和交接背景，不是当前继续执行指令；旧等待反馈条件已被本轮反馈取代。

用户已批准2026-09-12性能/UI执行Prompt；按A–F实施，当前基线、异常边界及下一步
见`PERFORMANCE_UI_PROGRESS.md`。交付为经验证的Mac本地源码、真实证据和人工清单。
依赖/PDF响应、三切片工作台视觉、任务面板及有实测收益的次级优化均在范围内；
未知验收不改为通过，不重开受限Biber、Windows或独立发布任务。完成条件以原Prompt
逐项验收为准，不用本轮已取得的基线替代性能和UI改进。
B1有界后台成员计算、监听更新及保存/编译前核对已实现，GUI预算有实测改善；
B2/B3的PDF身份复用、迟到视口/回调保护及紧凑搜索已实现；继续C–F。FINAL后处理
30%实验目标未达到，历史PDF空白和一次大工程输入尖峰不因部分指标达标而豁免。
C源码空间/默认折叠、共用编辑/PDF切换、交付固定底栏及Block按需辅助面板已实现；
离屏矩阵和相关回归有证据。用户确认前台后，三切片五档/28状态原生检查通过；
旧失活原因、完整尺寸/状态与人工验收仍保留，不宣告F通过。
C3补齐源码辅助面板与完整状态阅读，并修复Block指示器借用隐藏源码状态；真实
FINAL/固定审阅已有原生证据。E1局部字数片段累积优化已保留，长英文文本交替对照
约快18%，有峰值分配代价，计数结果不变。D1提交检查的摘要/原因/技术阅读与固定
动作已实现，离屏与当前12状态原生专项通过。D2分阶段交付也已实现，4类项目8条真实离屏
交付、固定字节/UNKNOWN/取消保护与五档键盘有证据；原生仍保留。D3大纲/引用路径/
公式键区/Block标签与完整身份已局部实现；共用检查点/恢复/迁移补看后已修复固定
动作和清单空间，仍保持不同事务。E2已测启动/缩放/空闲：多窗口缩放热点是Qt全局
QSS应用，绘制暂停无收益、不采纳；不增加缺少证据的懒加载、GC或缓存机制。
F首次回归的分栏偏好与旧测试前提已处理；四尺寸五档又修复了公式高度、焦点出口
及Block默认比例，当前83状态离屏和最新源码全套通过。冻结源码最终性能已复核，
依赖GUI提交达标，多窗口缩放未达暂定目标。前台恢复后Goal为active，当前源码分段Cocoa
尺寸/审阅/搜索、双root归属、取消与失败恢复已补；live200-child末键415.477ms，输入
p95 29.494ms但仍有74.756ms显示观察尖峰。下一步完成剩余人工/视觉状态确认，并对
超出局部范围的全局样式优化另作决定；不重复稳定全套，不豁免250ms缺口与历史风险。
用户随后确认先做局部优化；布局整理后置和重复Dock声明精简均未显示足够收益，没有
采用。补视觉状态时确认主按钮焦点/禁用反馈缺失，已用现有配色局部修正并通过五档
像素/几何/草稿专项。全局规则候选全套通过但性能变慢；同进程对照中旧样式也变慢，
不声称因果闭环。最终收敛到具体主按钮的局部颜色样式，全局QSS恢复原内容；专项及
普通状态图像一致，最终full-r6及对应主按钮原生专项通过。空闲测量多窗p95约445ms、
GUI CPU约433ms，250ms仍未达到；不宣称提速，后续较大样式方案与人工门槛仍需确认。
用户已进一步授权结构性调整，要求最小实现和尽快交付。已采用固定QSS加五档尺寸
标记，保留原生成器/UiMetrics、控件实例与非菜单缩放兼容路径；不引入新依赖或通用
布局框架。当前专项/83状态离屏及1701项完整回归通过，30次多窗p95约101ms；
前台现已恢复，结构版83状态原生矩阵和10状态搜索通过；原生输入首轮p95为56ms，
一次CPU分段诊断仍有约70ms尾部等待，尚未定位具体原因。下一步仅处理明确的输入
热点及人工验收缺口，不重复已通过的全套/布局矩阵，不抹去首轮失活或历史异常。
同窗30对等待方法比较后，正常Qt循环仍有74–76ms尖峰，未采用测量方式替换来宣布
达标。系统负载/压缩活动的后续快照不足以证明原因，不无限重复基准或关闭用户程序。
用户已确认并完成短时验收：原口径30次输入p95为43.290ms、max91.936ms，末键
成员396.988ms；本轮p95达标不抹去尾部和跨轮波动。Goal恢复active、未完成，
不再将等待同一时段列为阻塞。接下来需要定位执行位置的新证据及原人工清单，
不升级运行时或替换渲染器；当前简明交接和完整历史链接见PERFORMANCE_UI_PROGRESS。
新增事件位置诊断后，已移除100%/非菜单档无作用的全局缩放过滤器；当前专项及
完整回归通过，原生同窗对照仅小幅改善，独立复测仍有长尾。继续定位具体执行路径，
不把默认档小改扩成全面样式重写，也不由配对通过关闭此前失败。
最后将输入时标签页路径解析移到注册/保存边界，保留原子写入、状态库和FINAL校验。
全套暴露的深层子文件兼容问题已修复；末轮仅旧反馈路径预期失败，修正并专项通过，
未重复全套。当前进入源码交接，停止自主追加实验；最新原生复验、尾部/历史风险和
人工清单未完成，后续继续或阶段结束由用户决定。
用户随后授权本机更新；当前Beta 2本地验收应用已安装并启动，preflight通过，旧版
完整备份保留。下一步由用户验收，不继续自主追加优化；未扩展远端更新或公开发布。
验收反馈连续三个Goal轮次尚待用户提供；Goal已设为blocked，未标完成。收到明确
反馈后只处理反馈或用户确认的下一步，不因自动续跑消息重新开始测试。

## Current Source Acceptance - Response and Compile Optimization

已授权的响应优化工作源码进入维护者验收；当前实现和测量证据见
`docs/response-optimization-verification-2026-09-08.md`，机制约束见 D018。

- 后续真实项目验收覆盖连续输入、未打开依赖、外部冲突、多 root 和正式导出；不把
  合成样本时间当作真实项目或 Windows 的性能承诺。
- 大图继续内容哈希与原图 FINAL；仅在新的测量证明收益且失效规则完整时评估缓存。
- 大型依赖图的 GUI 扫描和超大文档统计保留为独立后续任务。
- Preview 双向 SyncTeX 已按 D019 校验当前显示构建；源码验收
  覆盖真实图片代理、原始子文件、缩放/滚动、过期结果与正式导出非回归。
- 本地应用替换、版本选择、打包和公开发布均保持独立维护者门槛。
- 公式与内置表格的交互更新已获独立源码授权：优先修复输入/预览一致性、草稿撤销、
  空白和零值保留、行列标识与超限拒绝；继续沿用本地纯规则和显式提交边界，不扩展
  为通用数学排版器或完整电子表格。公式结构键按补充反馈统一为细线空心图示，
  保持按钮动作不变；源码验收后再决定是否更新本机应用。

完成条件：维护者明确选择验收项目或下一发布/升级任务，再建立新的 bounded brief。

## Independent Release Gate - Preserve the Accepted Artifact Boundary

目标：把已验证源码变成可解释、可复现、跨平台一致的下一版本。

- 建立并执行新的项目状态、路线图、决策和记忆管理规范。
- 冻结已验证的双保真 preview/final、正式导出与图片缩略图 source delta；仅收口
  已确认的编辑阻塞问题、普通模式左右/上下/2×2 图片布局和高频命令补全，不继续
  扩展为自由画布或通用语言服务器。
- 收口项目文件工具箱：稳定项目树根、展示论文与图片资源、支持 `.tex` 拖拽/新窗口、
  图片安全拖入，以及项目内受控重命名和移动；不加入删除或静默引用重写。
- 旧 post-0.2.7 / `0.2.8` 推荐已不适用；当前源码与公开制品版本由
  `docs/PROJECT_STATE.md` 区分，下一正式版本由维护者选择，不因 V1 工作代号改号。
- 重新生成并验证 versioned macOS DMG 与 clean source archive。
- 在 Windows-local path 构建同版本 ZIP/installer，并验证：
  - 无系统 Python 时可启动；
  - UI 报告正确版本；
  - 能检测 MiKTeX 或 TeX Live；
  - 基础 source-to-PDF 流程成功。
- 保持已通过 WEB-001～004 的静态官网及其 Release 数据完整性；任何公开 Pages
  部署仍需维护者明确批准并执行受保护的发布流程。
- 发布分支制品仍受独立验收门槛约束；已获授权的下一代本地源码开发按
  `docs/CODEX_V1_DEVELOPMENT_INSTRUCTIONS.md` 和 `docs/V1_IMPLEMENTATION_PLAN.md`
  执行，不把发布暂缓误当成本地功能开发禁令。

完成条件：源码、文档、macOS artifact、Windows artifact 和版本号一致，且
`docs/PROJECT_STATE.md` 已记录最终验证状态。

## Implemented Source - Submission Readiness

目标：用已有能力组成第一个真正的“科研写作基础设施”纵向工作流。

普通源码与 Block 的本地只读提交检查已经实现；实际状态和源码身份见
`docs/PROJECT_STATE.md`，逐项证据见 `docs/V1_ACCEPTANCE_MATRIX.md`。检查组合：

- 当前源码是否已保存；
- 最新 compile root 是否成功编译；
- 当前 PDF 是否为最新版本；
- Word Count 数值、目标范围和统计模式；
- 未解析 citation/reference/label；
- 缺失图片、BibTeX 文件和常见 package；
- toolchain、encoding 和 build environment 状态；
- 最终可交付 PDF 路径与生成时间。

实现原则：优先复用 `word_count`、`diagnostics`、`pdf_state`、`paths` 和
Environment Doctor，不另建平行状态系统。

验收边界：M1 的只读/身份/失败路径已有证据；真实学生对提示的理解仍待真人验收。
不把这个纵向切片的完成当作整个 V1 或正式发布完成。

## Closed Local Stage - Ordinary Mac Writing Source Handoff

M2 工作台/可编辑配置、M3 引用/素材/来源、M4 检查点/恢复/迁移、M5 固定交付链
均已有工作源码和分层证据，不再作为需要从零实现的建议。用户于 2026-09-12 明确
优先体验 Mac 普通写作界面，随后明确按 Mac 源码阶段性交付收尾，未完成验收留到
后续。源码入口与已知限制已交接，见 `MAC_WRITING_HANDOFF.md`；Agent/MCP 受限
Biber 支持延期。等待下一 Goal，不自动继续以下保留项或新增性能开发。
原始 M6 验收要求仍保留，后续可选范围包括具体阻塞和未关闭证据：

- 迁移副本确认打开后的可见性/生命周期及退出模态后的激活修复已有证据，不重复
  该检查；处理剩余 OS/menu 纯键盘证据边界。Block 100%/150% 实际入口、迁移发布、150%
  交付/检查点/恢复控件已有证据，不重复这些流程及此前通过的源码/输入法/
  控制台/PDF 场景。具体缺口见键盘后续收据；AX 菜单不标成纯键盘通过。
- 处理有限生命周期风险；受限 LuaLaTeX 的授权 Mac 后端已接入共享源码并完成
  LuaLaTeX/BibTeX、真实 stdio 导出和进程树停止的分阶段证据。不重跑已通过链，
  Biber 作为延期项保留已知失败；其他安装布局和弃用接口的发布支持边界不变。
  不以 Biber 为普通 GUI 源码体验的前置门槛。见当前实施计划和 R5 接入收据。
- 核对当前源码与证据，维护发布准备清单，明确 Windows、真实存储故障、真人和发布门槛。

阶段收尾范围在 `docs/V1_IMPLEMENTATION_PLAN.md`；有限剩余项在
`docs/V1_ACCEPTANCE_MATRIX.md`。此次按用户调整的范围完成阶段交付，不将剩余项
改成通过；原完整 V1 验收和首发门槛保持独立，不建议 release candidate。

## Next - Project Session Boundary

目标：降低 `MainWindow` 的共享状态复杂度，但不重写稳定 GUI。

- 先定义最小的 project/session state：compile root、open documents、source
  revisions、PDF record、compile manager 和 project profile。
- 一次只迁移一个现有工作流，并保留兼容门面。
- session model 不直接持有 QWidget；Qt signal wiring 继续留在 GUI 层。
- 每次迁移都必须有针对多标签页、root/child 和后台编译的回归测试。

该方向目前是 architecture proposal；开始实施前应在
`docs/DECISION_LOG.md` 中转为 Accepted decision。

## Implemented Draft Editing - Source-First Formula Composer

目标：让学生用二维结构和键盘高效构造公式，同时保持 LaTeX 源码可见、可复制、
可撤销且不被静默改写。

纯规则、隔离 GUI 草稿、package edit plan、显式确认、revision 与一次 Undo 已在
源码实现并有专项验收，见项目状态。旧“只实现 core”的阶段描述不再代表当前能力。
不引入 MathLive/QtWebEngine 或通用 WYSIWYG；D013 的完整跨平台、IME、成本和
架构 promotion gate 仍未因此自动关闭。M2 复用并验证既有编辑器，不重新实现。

分阶段条件：

1. 纯 Python wrapper/parser/template/edit-plan 规则与异常输入测试通过。
2. 本地可视技术原型通过 macOS/Windows 制品、离线、内存、IME 和许可证检查。
3. 插入/替换是一次 Undo；Cancel 零修改；revision 冲突拒绝覆盖。
4. 未知宏和复杂环境可无损退回源码模式；最终正确性仍由正式编译确认。

长期边界和 promotion gate 见 `docs/DECISION_LOG.md` D013。

## Implemented Local Profiles - Later Course Content

目标：以数据驱动方式支持不同学术项目，而不是把学科规则硬编码进 GUI。

可编辑、可禁用且不执行脚本的本地 profile 已在 M2 实现，包含用户配置的引擎、
目录建议、字数目标和检查选项。其来源/边界见 `v1-m2-profile-verification-2026-09-10.md`。
尚未进入实施的课程内容候选：

- IA、EE、实验报告和课程论文的 project profile；
- 有适用版本和权威来源的课程特定模板与规则内容；
- 本地 milestone/checklist；
- 可导出的项目健康报告，便于教师或技术支持排查环境问题。

所有 profile 必须可编辑、可禁用，并明确区分“技术检查”和“学术评价”。

## Distribution Improvements

Mac arm64 Beta 3 线上更新已交付：安装租约、原生中断/恢复、真实 HTTPS 检查下载与
重启替换已验证，GitHub资产与Vercel签名appcast已发布。本机已完成首次bootstrap安装。
证据和限制见[`BETA3_DELIVERY.md`](BETA3_DELIVERY.md)，后续只处理用户具体反馈，不重跑
已完成矩阵或重新把临时协调进程列为待授权设计。

更新分发实现与发布边界见
[`update-delivery-design-2026-09-08.md`](update-delivery-design-2026-09-08.md)，
长期决策见 D020。中文设置、应用级调度、全窗口保存退出和 Sparkle/WinSparkle
薄适配器已在工作源码实现；普通源码和默认打包仍不启用更新。

旧r2保留为历史证据，不作为新版本发布。Apple Developer ID/公证与 Windows 分架构
EXE 安装器仍为后续独立任务。旧手动版用户需先安装带更新器的版本一次；之后可在
应用内更新。接入步骤见 `packaging/UPDATES.md`，安装协议见 D026。
差分、运行时渠道切换、自动回滚与百分比灰度后置；不触发 GitHub Actions。

在用户需求和发布资源允许时评估：

- Windows 持续验证环境；
- macOS Developer ID signing / notarization；
- Intel 或 Universal 2 macOS 支持；
- 更清晰的安装和 LaTeX distribution onboarding。

这些工作不能通过降低构建可复现性或隐藏安全警告来完成。

## Explicit Non-Goals

以下方向当前不进入路线图：

- 将 ICSTeX 变成云端编辑器；
- 静默上传论文、日志或引用数据；
- 默认开启 AI 自动改写或自动修改学生正文；
- 在安装包中捆绑完整 MacTeX、TeX Live 或 MiKTeX；
- 直接编辑 PDF 或从 PDF 反向生成 LaTeX；
- 在没有明确扩展需求前建设通用插件系统；
- 为增加功能数量而复制 Overleaf 或大型 IDE 的全部能力。

## Delivered - Agent / Harness MCP Boundary

本地 stdio MCP 已提供项目检查、文档 CAS 读写、搜索/诊断/字数/引用/SyncTeX、
Block CRUD/布局/主题/来源/组装、受限编译、本地 OCR candidate、DOI/arXiv 元数据和
受权目录导出。协议调度复用官方 MCP SDK 2.x；同项目读取有界并发，修改/编译/导出
保持 FIFO 独占，不同项目通过独立命名实例并行。Skill 只描述正确工作流，权限、
路径与并发保护由 core 强制。

后续只在真实工作流缺口出现时增加语义能力；不加入 GUI 遥控、后台 daemon、任意
文件系统访问、Agent 自授权、OCR 自动落盘或通用插件系统。

## Delivered - Guarded Project File Toolbox

文件树以每个窗口的 canonical project root 为稳定边界，展示 `.tex`、`.bib`、常见
图片和子目录。`.tex` 可双击或拖拽打开，并可从上下文菜单进入独立窗口；图片拖拽
复用现有安全导入事务。文件模型保持只读，重命名和移动由项目级控制器检查越界、
重名、符号链接、编译占用及 LaTeX 相对引用后执行。被引用路径 fail closed，不自动
改写论文源码。

## Feature Admission Checklist

进入实现前必须回答：

- 它解决哪个具体学生任务或风险？
- 能否复用现有状态和模块？
- 失败时是否会损坏、误报或泄露用户内容？
- 是否可以用纯规则或本地工具完成？
- 如何验证正确性和非回归？
- 它增加的长期维护成本是否低于用户价值？

无法回答以上问题的功能停留在 idea，不进入 active assignment。
