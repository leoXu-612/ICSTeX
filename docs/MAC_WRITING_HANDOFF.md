# Mac 普通写作界面：本地源码交接

日期：2026-09-12（Asia/Taipei）

本阶段已按用户明确确认的“Mac 源码阶段性交付”收尾，未完成验收留到后续。
现有 Mac 普通写作源码已提供并按用户要求启动；Agent/MCP 受限编译中的 Biber
支持延期。这份交接不是安装包、稳定版承诺或完整 V1 验收。已安装应用未被本轮更新。

## 启动

在这台 Mac 的终端运行现有源码入口：

```bash
cd /Users/leo.xu/Desktop/Codex/ICS-Project-/ICSTeX
python3 -m app
```

不需要为本次交接重新安装依赖。请从仓库启动，不用 Dock 中的旧安装版判断本轮
开发成果。退出时按界面处理未保存草稿；不要强制结束进程。

## 已有写作流程

- 创建/打开项目、编辑 LaTeX、中文输入、保存和 Undo/Redo。
- 公式、表格和图片插入；引用、素材、Word Count 与提交检查。
- 本地编译、PDF 预览和明确的 FINAL/导出；失败或过期状态不冒充新成果。
- 项目检查点、独立草稿恢复及恢复到新目录；原项目保留。

这些流程已有各自源码身份下的测试和合成原生证据，不是本轮新跑的真人验收。
具体映射见 [验收矩阵](V1_ACCEPTANCE_MATRIX.md)，操作说明见 [用户指南](user-guide.md)。
当前源码摘要、最后完整回归及制品状态以 [项目状态](PROJECT_STATE.md) 为准。
此前已按用户要求启动 Python 源码，原生欢迎界面与 LaTeX 环境就绪状态已观察到；
这不是全部真人验收通过的证据。本次收尾只核对摘要并更新文档，没有重跑测试、
重启应用或接管前台。

## 已知限制

- Biber 的已知失败属于 Agent/MCP 受限编译通道；普通 GUI 编译策略未改变。
  延期不表示普通 GUI 的所有 Biber 项目都已通过验证，也不会自动替换为 BibTeX。
- 系统菜单的全程纯键盘操作证据仍缺失；现有工具路由问题不能证明真人菜单失效。
- 历史 Qt 计时器/样式崩溃尚未完整归因；已有局部修复不等于稳定性风险归零。
  完整 VoiceOver/辅助功能和真人使用验收仍未完成。重要文档先保留独立备份，
  初次体验优先使用副本；本任务不会修改学生正文。

Windows、打包、签名、替换安装版和发布不在本次交接范围。工作区增量尚未提交/
推送；无需改名的现有开发分支保持不变。等待用户下一个 Goal，再确定后续范围；
不自动重开已通过场景、延期验收、Biber 运行时或新增性能开发。
