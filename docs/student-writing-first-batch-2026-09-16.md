# 学生写作流程：第一批源码改动

本批按最小实现原则改善创建项目、状态反馈和错误入口；没有改编译调度，
没有新增依赖、网络请求或状态系统。未打包、安装、提交、推送或发布。

## 实际变化

| 环节 | 本批实现 | 复用的能力 |
| --- | --- | --- |
| 创建项目 | 按语言筛选既有用途模板，说明适用场景；需主动选择模板；保存位置和不覆盖目录提示保留 | 原模板、项目初始化事务、DialogComboBox |
| 排版示例 | 九个模板的真实首页静态图；源码和高级设置默认收起；自动选择保留 | CompileManager 离线生成、既有资源加载和焦点滚动容器 |
| 保存与预览 | 分开显示文档已保存、PDF 当前版本、仍显示旧版；自动开关名称对应真实快速预览/正式模式 | 原有 root/revision/build-id 及 PREVIEW/FINAL 状态 |
| 错误处理 | 问题、文件/行、下一步分栏；固定可见的定位与原始日志入口；缺图报错中文解释 | 原诊断模型、源码定位、只读日志 |
| 源码修复 | 显示将加入的宏包声明及完整差异，用户确认后才应用；取消或内容变化不修改 | 原 package_update、ensure_packages 和 Undo |
| 欢迎页 | 就绪环境紧凑显示；缺失时展开指引并显式重新检测；最近项目附最近文件与短路径 | 原环境检测、最近项目和文件记录，不递归扫目录 |
| 帮助 | 静态导引的 Close 改为“关闭” | 原帮助窗口 |

最近文件不冒充已确认的主文件。模板静态图不冒充当前项目 PDF；源文改变时，
摘要不匹配的示例会退回“暂无排版示例”。生成命令仅供开发使用：
`python3 tools/build_template_previews.py --workdir <新的证据目录>`；应用不调用它。

## 已取得的流程证据

证据根目录：
`/Users/leo.xu/.codex/visualizations/2026/09/16/icstex-first-task/`。

- `template-build-v2/`：九个原样模板全部通过实际本地编译与首页渲染。
- `final-ui-v3/`：隔离设置与示例项目，通过真实 GUI 编译入口创建中文项目，
  修改标题、保存、正式编译，再触发缺图错误；旧 PDF 保留且明确标为过期。
  `updated-pdf.png` 已观察到修改后的标题，`saved-but-old-pdf.png` 同时显示
  已保存源码和旧 PDF，`error-and-log.png` 显示错误、恢复建议及原始日志。
- `final-ui-v2/updated-pdf.png` 抓取早于异步绘制完成，白页截图不能当作渲染通过。
  后续仅修正采集脚本等待已绘制内容，再观察 v3 图像；未为此修改产品渲染代码。
- `latency-delivery/`：真实自动保存/预览链和当前版本像素核验；单页中文模板
  暖轮 3 次中位数 2219.516 ms。相同旧版的前后对照中位数为 2150.623 ms、
  2323.857 ms；样本有限且时段波动明显，不声明加速或统计显著的性能等价。
- 所有本轮窗口为 offscreen；不代表原生 Mac、IME、全新电脑或学生试用验收。
  缺失工具截图是显式模拟状态，没有卸载本机环境。

## 验证异常和边界

首轮完整回归在 247.397 秒时以 SIGABRT 退出；源码前后未变。原生报告
`/Users/leo.xu/Library/Logs/DiagnosticReports/Python-2026-09-16-152749.ips`
显示 malloc 无效释放，经 PySide 的 `QApplication.allWidgets()` 包装器转换触发。
这只定位了发生位置，没有证明根因，更不能称已修复。原日志保留在
`full-regression/`；后续使用同一现有回归工具的详细输出和 faulthandler 定位。

102 项新入口/缩放/界面专项未复现原生退出，其中旧的单个键盘测试在五个缩放档
仍预期“检查项目”直接到列表。新增“定位问题”已改变顺序，修正断言后该单项通过，
同时要求新按钮可见、正反 Tab 可达且文稿不改变。

最终完整回归结果以 `docs/PROJECT_STATE.md` 为准；原生退出不是靠后续重跑抹除的风险。
已通过且无关的升级专项没有另行重复运行。

详细输出轮完整执行 1770 项，没有复现原生退出，但有三项旧断言失败：最近项目
提示已增加“项目位置”，另两项以不完整的 `%PDF` 字节冒充可加载的预览。后两项改用
仓库已有的有效 PDF 夹具，并更新正式编译文案；该组 17 项/3.401 秒通过。
没有为了让测试通过而撤掉加载失败提示，也没有修改编译或 PDF 渲染实现。

最终冻结源码完整回归通过 1770 项/288.095 秒，命令 289.481 秒、退出 0；
compileall 和 diff 检查通过。收据在 `full-regression-final/`，前后身份一致：
app `808efbd743464e3f95e903445981f6f8c6015be314e371871b47652c009afe23`，
app+tests `48c0c7547c5ca6746f4c66a77cc39cc933e4293663316a4bae5ebfb8d878c7bc`。
Qt 提示和临时目录的异步读取诊断仍保留；不称零警告或全部原生稳定性已验收。

## 明确未做

可退出/重入的交互式示例导引、图片重新选择并预览替换、学生试用与全新电脑安装
验收留到后续。原生退出若再次出现，应依照具体测试/堆栈继续有界定位，不无证据地
更换 Qt、编译器或全局样式。公开 Beta 3 和 `/Applications/ICSTeX.app` 均未更改。
