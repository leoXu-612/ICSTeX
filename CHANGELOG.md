# ICSTeX 更新日志

## 2.1.0-beta.1 - 2026-08-08

Formula Intelligence（本地公式识别）：

- 结构化公式编辑器 2.0（可视化 + 源码双视图、草稿隔离、单次写回、本地撤销）。
- 可选 pix2tex 本地公式识别运行时：图片 → 识别 → 人工核对 → 写回文档；离线运行、不依赖云端。
- 统一批量图片识别窗口：队列、顺序识别、进度条、逐项状态（待识别/识别中/成功/失败/已取消/已精调）、
  单项重试、查看错误、从批次移除；按图片内容指纹去重（可勾选允许重复入队）。
- ROI 创建/移动/8 点缩放/删除解耦，ROI 变化防抖自动重识别 + 选中 ROI 单独重识别。
- 重复提交幂等、对话框打开防抖、aligned 多公式合并；识别循环按 request_id 隔离，互不提前结束。

UI Scale 与响应式布局：

- 新增统一 `UiMetrics`/`TypographyMetrics` 设计令牌与 `UiScaleManager`，视图 → UI Scale
  提供 90/100/110/125/150% 五档；欢迎页/标签栏/PDF 工具栏响应式重排。

Block 模块与主控制台集成：

- 单一 `ProjectSession` 收敛 registry/layout/sources/主题/表格模型/唯一 CompileManager/
  全局 QUndoStack/SelectionManager；主控制台嵌入 Block 导航/属性/诊断 Dock 与中央工作区。

公开发布安全边界：

- 打开项目不再自动编译；编译忽略项目 `.latexmkrc`、关闭 shell escape 并使用有限超时。
- Magic Root、Block 渲染、联网元数据与可移植导出采用默认拒绝的路径和信任边界。
- macOS、source 与 Windows ARM64 制品均已从安全修复后的源码重新构建。

更新：本版本为 macOS Apple Silicon（arm64）Developer Beta，正式稳定版发布前请保留备份。

## 未发布

项目文件与窗口：

- 文件工具箱保持在用户选择的项目根目录，不再因打开子目录中的 `.tex` 而缩小范围；
  现在显示 `.tex`、`.bib`、常见图片和子目录。
- 支持双击或拖拽打开 `.tex`，右键可在独立窗口打开；`Ctrl+Shift+N` 可新建窗口，
  关闭后会从窗口注册表移除。
- 图片可直接从文件树拖入已保存的文档，并复用既有后台复制、相对路径和失败回滚流程。
- 新增项目内重命名和移动；越界、重名、符号链接、内部构建目录、活动编译或会失效的
  LaTeX 引用都会拒绝操作，不自动改写论文源码。

编辑与图片布局：

- 修复补全列表用方向键移动后，回车或 Tab 仍插入首个候选项的问题；插入结果现在
  与弹窗高亮项一致。
- 命令补全从 23 个扩展到 68 个高频模板，覆盖 `\text{}`、`\textcolor{}{}`、
  文本样式、章节、交叉引用、引用、数学、单位、项目拆分与参考文献命令；插入后光标
  落在第一个待填写参数。`textcolor` 等颜色命令缺少 `xcolor` 时会给出可应用的诊断。
- 普通源码模式的“并排图片”升级为“图片布局”，支持左右、上下和 2×2 田字排列；
  每张图可独立设置宽度，保持原始宽高比，并阻止同一行宽度总和超出页面。
- 多图布局导入采用事务式复制；任一图片复制失败时回滚本次新增副本，不插入半成品布局。

Word Count：

- 修复中文句号、顿号等标点可能被 TeXcount 当作汉字计入的问题，并覆盖扩展区汉字。
- 重复插入同一子文件时按实际出现次数统计；循环引用仍会安全截断。
- 本地结构化统计不再把重音命令或词内格式命令拆成多个词，并支持 `%TC:ignore`、
  行内 verbatim、脚注及可选短标题/短说明的正确分类。
- “图表说明”改为“说明/脚注”，TeXcount 模式不再被表述为绝对精确；课程提交口径
  仍以对应要求为准。

## 0.2.7 - 2026-07-11

Word Count：

- 新增 Turnitin 风格的“文本分层预览”，用颜色区分有效正文、标题、图表说明、行内公式、行间公式和数字。
- Word Count 结果现在附带可视化片段，便于解释某段文本为什么被计入或排除在某一类统计中。
- 统计现在按编译 root 合并 `input`、`include` 和 `subfile` 子文件，并在预览中标注来源文件；已打开的未保存子文件通过内存覆盖参与统计，不会改写磁盘文件。
- 子文件统计复用 LaTeX 编码声明：可正确读取 CP1252/Latin-1 等已声明编码，无法解码时明确提示并跳过，不再用替换字符静默产生失真结果。
- 修复无未保存缓冲区时，`texcount -utf8` 可能直接读取 CP1252 子文件并拆散重音单词、导致总数虚高的问题。
- 无法读取或无法解码的子文件现在会在临时 UTF-8 影子项目中用空文件隔离，确保原始字节不会被 `texcount` 绕过警告后再次计入。
- Python 简化统计改用 Unicode 分词：重音字母和直/弯撇号单词不再被拆散，中文仍按字符统计。

## 0.2.6 - 2026-07-08

更新：

稳定性：

- 修复无 `% !TEX root` 的 `\input{child}` 子文件仍可能被当作独立 root 编译的问题；现在会复用父文档的 `CompileManager`。
- 修复“另存为”后旧 root 的编译器实例可能残留在窗口 registry 中的问题。

模板与新手体验：

- 模板库新增 Extended Essay、Physics IA、Math IA、化学实验报告、中文 XeLaTeX 文章。
- 模板面板支持把当前文档保存为个人模板、导入 `.tex` 模板、导出所选模板。
- 欢迎页和帮助菜单新增“新手导引”，离线说明首次安装、写作、编译、排错和导出流程。
- 插入工具新增章节标题、分段函数和引用块。

界面：

- 优化模板面板、导引弹窗和帮助文本样式，使工具箱更像连续工作台的一部分。

## 0.2.5 - 2026-07-06

PDF 状态与编译可靠性：

- 每个 LaTeX root 独立跟踪 PDF 新鲜度（尚未编译 / 待更新 / 正在编译 / 已是最新 / 显示上次成功版本 / 暂无可用 PDF），PDF 预览上方新增常驻状态条。
- 修复切换标签后可能导出另一文档 PDF 的问题：切到未编译文档会清空预览并禁用导出；每个标签只加载、导出自己 root 的 PDF。
- 导出 PDF 改为原子写入，失败不会留下损坏的半成品文件；导出较早成功版本前会明确提醒。
- 编译结果分为八类（成功 / 已停止 / 未找到编译器 / 根文件不存在 / 无法启动进程 / LaTeX 错误 / 未生成有效 PDF / 内部错误），状态栏和日志用统一的中文标题与建议；LaTeX 错误优先显示第一条可操作诊断。
- 手动停止编译在 macOS 和 Windows 上都能被准确识别为“已停止”，不再依赖平台相关的退出码。
- 外部程序修改源码并重载后，立即标记“源码已修改，PDF 待更新”并按所属 root 重新编译。
- root 与子文件（`% !TEX root`、`\input`、`\include`、`\subfile`，支持多层嵌套）共享同一编译器实例和 PDF 状态，不再对同一构建目录并发编译；编辑深层子文件会正确标记 root 的 PDF 过期。
- 后台文档的编译不再影响当前文档的状态栏、计时器、进度条和停止按钮；切换标签时这些指示随当前文档同步。
- 清理编译缓存会使仍在结束中的旧编译结果失效，状态不会从“尚未编译”回弹为“编译失败”。
- “另存为”改变编译 root 后立即清空旧 PDF 并重新绑定状态。
- PDF 双击反向定位只使用当前 root 的 PDF；Finder/文件管理器无法启动时给出提示而不是报错。

## 0.2.4 - 2026-07-04

界面优化：

- 重整浅色工作台的色彩、间距、面板标题和分隔线，加入克制的 ICSTeX 深红品牌强调色。
- 主工具栏收敛为项目、工作区、编译和检查四组核心操作，低频命令仍保留在菜单中。
- 使用随 App 打包的 Lucide 线性图标替换跨平台表现不一的 Qt 系统图标。
- 工具箱的 9 个横向页签改为可伸缩的垂直导航，保留文件、大纲、搜索、图片、历史、插入、模板、引用和标签全部功能。
- PDF 工具栏改为紧凑图标控件，并增加未编译时的空状态提示。
- 状态栏增加引擎、自动/手动编译和编译结果状态；错误与项目检查页签显示数量。
- 编译控制台新增标题栏收起/展开按钮，收起后仍保留可操作的标题栏。

## 0.2.3 - 2026-06-30

更新：

- 帮助菜单新增“一键复制反馈包”，无需先打开环境医生；反馈内容继续排除论文正文和绝对路径。
- Word Count 新增六类彩色相对分布条，区分有效正文、标题、图表说明、行内公式、行间公式和数字，并保留精确数值。
- 可视化表格编辑器支持从已有 LaTeX `tabular` 代码反向解析填表。
- 可视化表格编辑器支持直接粘贴 Excel / Numbers / Google Sheets 单元格或 CSV 自动填表。
- 引用导入新增可选的“联网获取真实元数据（仅 DOI/arXiv）”，默认关闭，离线生成骨架仍是默认。

隐私：

- 快捷“复制反馈包”不再携带可能回显源码和本机路径的原始 TeX 日志；详细日志仅在用户主动打开“环境医生”时提供。

修复与稳定性：

- BibTeX 追加和项目初始化改用原子写入；图片复制只在完整成功后才进入 `figures/`，失败不留半截文件；损坏的历史快照不再以替换字符恢复到论文。
- 打开和外部重载 `.tex` 时不再用替换字符静默吞掉非 UTF-8 内容；现在会识别 UTF-8 BOM、TeX 编码声明和 `inputenc`，必要时让用户选择编码，并按原编码安全保存。
- Windows/macOS 打包依赖安装会临时忽略已失效的本机代理，并可自动使用项目根目录中的 `pylatexenc` wheel 作为离线兜底，不修改系统代理设置。
- 关闭含未保存修改的标签页或主窗口时，提供“保存 / 放弃 / 取消”三种选择；多标签关闭采用延迟清理，后续标签取消时不会提前停掉其他文档的保存和编译。
- 停止编译时确保编译进程真正退出，避免切换引擎/清理缓存时与旧进程争抢构建目录产出损坏 PDF。
- 后台标签页编译完成时不再把 PDF/错误结果覆盖到用户当前正在看的文档上。
- 修正外部文件变更判定，不再因“保存后短时间窗口”而静默吞掉真实的外部修改。
- 在已打开文档中修改 `% !TEX program` 注释后，下次编译即生效，无需重新打开。
- 修复文件监听在目录内最后一个文件移除后仍泄漏系统级目录监听的问题。
- 修复插入表格、超链接并自动补 package 后，源码区可能保留旧横向布局而不立即自动换行的问题。
- 文件监听改用稳定的跨平台轮询后端，避免 macOS FSEvents 在快速增删监听时导致原生崩溃。

注意：

- ICSTeX 仍不内置 MacTeX / MiKTeX / TeX Live，首次使用前需先安装 LaTeX 发行版。
- macOS 版仍未签名、未 notarized，首次打开可能需要在“隐私与安全性”中选择“仍要打开”。
- 当前 macOS DMG 仅为 Apple Silicon `arm64` 版，未生成 Intel / Universal 2 产物。
- Windows 便携包/安装包必须在 Windows 虚拟机或实机中重建验收；不使用 macOS 产物冒充 Windows 发布包。

## 0.2.2 - 2026-06-15

更新：

- 新增源码自动换行，长代码行会根据窗口宽度自动折行，减少左右拖动。
- 设置中新增“源码自动换行”开关，用户可以按习惯开启或关闭。
- 优化多文件 LaTeX 项目的主文件识别，打开章节文件时更容易自动找到 `main.tex`。
- 优化环境医生反馈包，便于排查编译器、Word Count、root 文件等问题。
- 修复并改进了一些已知稳定性问题。

注意：

- ICSTeX 仍然不内置 MacTeX / MiKTeX / TeX Live，首次使用前需要先安装 LaTeX 发行版。
- 当前版本仍是同学测试版，macOS 可能提示“无法验证开发者”。

## 0.2.1 - 2026-06-12

更新：

- 优化环境医生和反馈包。
- 优化 Word Count 状态提示。
- 优化 macOS DMG 打包流程。
- 修复了一些跨电脑测试时发现的问题。

## 0.2.0 - 2026-06-10

更新：

- App 正式命名为 ICSTeX。
- 加入 App 图标和 macOS DMG 测试包。
- 加入插图、表格、模板、引用和标签等初学者辅助功能。
