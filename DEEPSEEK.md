# DEEPSEEK.md

DeepSeek and every other coding agent must follow `AGENTS.md`; this file is a
compatibility entry point, not a second engineering manual.

## Read Order

1. Confirm `pwd -P` is `<HOME>/Desktop/Codex/ICS-Project-/ICSTeX`.
2. Read `AGENTS.md` completely.
3. Read `docs/PROJECT_STATE.md` for current verified facts and the release gate.
4. Read `FORDEEPSEEK.md` for the current bounded assignment and its activation
   status.
5. Read `docs/DECISION_LOG.md` and `docs/ROADMAP.md` when the task affects
   architecture, product scope, or release order.

Do not start an assignment marked `QUEUED` or `BLOCKED`. Do not upload user
`.tex`, `.bib`, PDF, log, screenshot, or other research content to any external
service. Project memory rules are defined in `docs/MEMORY_MANAGEMENT.md`.

