# macOS 应用内更新交付

2026-09-15：准备任务已转为完整实施并交付。用户要求完成剩余开发直到线上更新可用；
安装期协调、打包、签名、公开资产、Vercel appcast 和真实 HTTPS 升级均已完成。

当前状态以 `PROJECT_STATE.md` 为准，公开版本及验收证据见 `BETA3_DELIVERY.md`，
安装协议见 D026 和 `UPDATE_INSTALLATION_GUARD.md`。不要再把安装保护列为待授权方案，
也不要用旧 r2 候选代替现有性能/UI源码。

应用保持默认离线。用户开启后启动延后检查，短间隔重启去重，运行期间每天复查；
下载和保存退出安装仍需确认。配置/公钥固定在包内，没有运行时换源或常驻服务。
首个带更新器的版本需要手动安装一次，本机已从匿名下载的公开ZIP完成该步骤，
完整旧应用备份保留。

Beta 3 仍为 arm64/ad-hoc、未 Apple 公证；Windows、Intel Mac、Developer ID、
自动回滚和全部安装权限布局不属于本次验收。未知交接可能需重启Mac再验证完整性，
损坏包需使用可信完整旧包恢复。不要把这些限制隐去，也不要擅自扩展后续任务。
